-- Удаление сообщений (правка продукта поверх ТЗ v5: было «удалений нет»).
-- Мягкое: текст остаётся в БД, в интерфейсе — заглушка «Сообщение удалено»
ALTER TABLE messages ADD COLUMN deleted_at TIMESTAMPTZ;
